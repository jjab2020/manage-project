<div>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}

    <button class="btn btn-primary">{{ button_label|default('Save') }}</button>
    <a class="btn btn-info" href="{{ path('userList') }}">Back to list</a>
</div>